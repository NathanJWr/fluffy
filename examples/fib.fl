var system = new System;

var fib = fn(n) {
    if (n < 2) {
       return(n);
    }
    return(fib(n-1) + fib(n-2));
}


var results = [ fib(5),
                fib(7),
		fib(9),
		fib(11),
		fib(13),
		fib(15) ];

system.print(results);

